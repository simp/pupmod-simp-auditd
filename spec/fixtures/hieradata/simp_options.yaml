---
# simp_options::package_ensure and simp_options::syslog, which 11.0.0 no
# longer reads. The values are deliberately distinct from the class defaults
# ('installed' / false) so the unit test proves they are ignored.
simp_options::package_ensure: 'latest'
simp_options::syslog: true
